/// @file main.cpp
/// @brief Interactive 3D Map Renderer – Entry Point
///
/// This stub verifies the build system links correctly.
/// Subsequent tasks will replace it with the full SDL+Vulkan main loop.

#include "core/debug_log.h"

int main() {
    DEBUG_LOG("map-renderer stub starting...");
    return 0;
}
